# AI History Native Ownership

AI history reads, transcript page reads, session pinning, deletion, runtime
session mappings, and migration/read support for legacy rows are owned by the
Rust sidecar.

This file used to describe rollout flags. Those flags have been retired; the
native backend is required.
